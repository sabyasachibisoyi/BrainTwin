# Environment
.env
venv/
.venv/
.testvenv/
env/

# Data (don't commit your knowledge base)
data/chroma/
data/images/
data/*.db
# Local capture pipeline (URLs, text previews, browsing-derived content)
data/captures.jsonl
data/capture_failures.jsonl
data/telegram_state.json
data/enrichments.jsonl
# Phase 2.5 Fix 2 — capture hydration sidecar (joined to captures.jsonl)
data/hydrations.jsonl
# Phase 3 — JSONL→SQL migration failure log (per-run, local)
data/migration_failures.jsonl
# Phase 2.5 Fix 3 — Whisper model files (~250 MB each, per-machine)
data/models/
# Phase 2.5 Fix 3 — vendored binaries if a future setup ever drops them
# here. Default install uses brew so this dir stays empty for most.
bin/

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.eggs/
.pytest_cache/

# IDE
.vscode/
.idea/
.cursor/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
